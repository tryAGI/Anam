# Anam.CLI

Command-line interface for the Anam SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Anam.CLI --prerelease
```

## Usage

```bash
anam --help
anam api --help
```